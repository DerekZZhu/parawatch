export default function NotFound() {
    return(
        <main className="not-found">
            <h1>Watch not found</h1>
            <p>Unfortunately this thread was not found. It may have been deleted.</p>
        </main>
    )
}